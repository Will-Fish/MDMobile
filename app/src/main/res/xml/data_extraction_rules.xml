<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Include only the files that should be backed up to Google Drive
        or excluded from backup. For guidance, see
        https://developer.android.com/guide/topics/data/autobackup -->
        <include domain="sharedpref" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="external" path="."/>
    </cloud-backup>
    <!--
    TODO: Define device transfer rules for Android 14 (API level 34) and higher.
    <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer>
    -->
</data-extraction-rules>